import Navbar from "@/components/Navbar";
import { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <main className="h-screen w-screen">
      <Navbar/>
    </main>
  );
};

export default Home;
